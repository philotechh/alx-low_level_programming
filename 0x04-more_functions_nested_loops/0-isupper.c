#include <stdlib.h>
#include "main.h"
/**
 * _isupper - funcion that checks for uppercase character
 * @c: character to be checked
 * Return: 0.
 */

int _isupper(int c)
{
	char ch = 'A';

	if (isupper(ch))

		printf("%d\n");
	else
		printf("%d\n");
	return (0);
}

